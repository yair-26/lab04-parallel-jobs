name: CI  
on: [push, pull_request]
jobs:
  test:
     runs-on: ubuntu-latest
     steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: download dependencies
        run: npm ci
      - name: run tests
        run: npm test
  lint:
     runs-on: ubuntu-latest
     needs: test
     steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: download dependencies
        run: npm ci
      - name: run linter
        run: npm run lint   